services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app/src
    command: uvicorn robot_rest_service.app:app --host 0.0.0.0 --port 8000 --reload
